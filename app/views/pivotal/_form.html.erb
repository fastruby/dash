<div class="token-form">
  <%= form_with model: current_user, url: pivotal_path, local: true do |f| %>
    <%= f.label :pivotal_token %>
    <%= f.text_field :pivotal_token %>
    <%= f.submit pivotal_button_text, class: "button"%>
  <% end %>
</div>
<div class="explanation">
  <h4>To make a new token follow these steps: </h4>

  <ol>
    <li>Open <%=link_to "pivotaltracker.com", "https://www.pivotaltracker.com/", target: :blank%> and sign in. </li>
    <li>Go to your profile and scroll down to API token.</li>
    <li>Click on "create new token" and copy the token.</li>
    <li>Enter token in field below (it will be encrypted when saved).</li>
  </ol>
</div>


